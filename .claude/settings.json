{
  "permissions": {
    "allow": [
      "Bash(./scripts/check.sh)",
      "Bash(uv run pytest *)",
      "Bash(uv run ruff *)",
      "Bash(npm test *)",
      "Bash(npm run lint *)",
      "Bash(npm run build *)",
      "Bash(git status *)",
      "Bash(git diff *)",
      "Bash(git log *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.py) cd \"$CLAUDE_PROJECT_DIR/backend\" && uv run ruff check --fix --quiet \"$f\"; uv run ruff format --quiet \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "ruff format"
          }
        ]
      }
    ]
  }
}
